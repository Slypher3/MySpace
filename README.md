# MySpace

A starter repository created for you.

## What I added

- `README.md` — this file
- `LICENSE` — MIT placeholder
- `.gitignore` — common ignores
- `src/main.py` — simple starter script

## Next steps

Initialize git, commit, and push to your remote:

```bash
git init
git add .
git commit -m "Initial commit"
# create repo on GitHub and add remote, then:
git remote add origin git@github.com:YOUR_USERNAME/MySpace.git
git branch -M main
git push -u origin main
```
